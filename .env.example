# Postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=context_cloak
POSTGRES_USER=mcpuser
POSTGRES_PASSWORD=changeme-in-production

# MCP Server
MCP_SERVER_HOST=0.0.0.0
MCP_SERVER_PORT=8080

# vLLM endpoint (behind BIG-IP)
VLLM_ENDPOINT=https://vllm.lab.local/v1

# BIG-IP (for reference only — Terraform uses tfvars)
BIGIP_MGMT_HOST=192.168.1.245
BIGIP_MGMT_PORT=443
