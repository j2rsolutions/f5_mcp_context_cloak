apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: context-cloak
type: Opaque
stringData:
  # TODO: Replace with real secrets in production. Use sealed-secrets or external-secrets-operator.
  POSTGRES_USER: mcpuser
  POSTGRES_PASSWORD: changeme-in-production
  POSTGRES_DB: context_cloak
