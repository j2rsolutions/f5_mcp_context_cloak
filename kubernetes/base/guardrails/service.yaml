apiVersion: v1
kind: Service
metadata:
  name: ai-guardrails
  namespace: guardrails
  labels:
    app: ai-guardrails
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: ai-guardrails
