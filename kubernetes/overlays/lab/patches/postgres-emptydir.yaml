apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: context-cloak
spec:
  template:
    spec:
      volumes:
        - name: data
          emptyDir: {}
          persistentVolumeClaim: null
